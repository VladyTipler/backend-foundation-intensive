# SQL practice

This folder is intentionally mostly empty.

Course exercises should create schema, tables, constraints, indexes and migrations here (or in your ORM migration folder).

Seeders never create the learning schema for you. They only validate it and insert test data.
