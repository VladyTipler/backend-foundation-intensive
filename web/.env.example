# Optional. Defaults work without copying this file.
API_PROXY_TARGET=http://127.0.0.1:3000
# Public URL used by the browser. Keep /api for the development proxy.
# Set http://localhost:3000 ONLY for the explicit CORS exercise.
VITE_API_BASE=/api
# Never put database passwords, session secrets or API keys in VITE_*.
